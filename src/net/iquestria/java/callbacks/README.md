net.iquestria.java.callbacks
----------------------------

This package is full of OkHttp `Callback`s which take the raw JSON data from the server and put it into a model. It then passes that model onto another callback. What?